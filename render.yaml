services:
  - type: web
    name: a-share-market-data-service
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python scripts/run_market_data_server.py
    plan: free
    healthCheckPath: /health
